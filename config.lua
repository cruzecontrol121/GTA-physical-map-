Config = {}

-- Item name as it appears in qb-core/shared/items.lua.
Config.ItemName = 'world_map'

-- Controls how many milliseconds the player waits while unfolding the map.
Config.UnfoldDuration = 2000

-- Optional animation when using the map item.
Config.Anim = {
    Dict = 'amb@world_human_tourist_map@male@base',
    Name = 'base',
    Flag = 49,
}
